<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
 
  <!-- 外部文件 -->
  <properties resource="db.properties" />
   
    <!-- sql实体类别名 -->
  <typeAliases>
  	<typeAlias alias="student" type="com.wood.entity.Student"/>
  	<typeAlias alias="teacher" type="com.wood.entity.Teacher"/>
  </typeAliases>
  
  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="${mysqldriver}"/>
        <property name="url" value="${mysqlurl}"/>
        <property name="username" value="${mysqlname}"/>
        <property name="password" value="${mysqlpwd}"/>
      </dataSource>
    </environment>
  </environments>
  
  <mappers>
    <mapper resource="com/wood/sql/studentMapper.xml"/>
    <mapper resource="com/wood/sql/teacherMapper.xml"/>
  </mappers>
</configuration>